<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <parent>
        <groupId>org.sonatype.oss</groupId>
        <artifactId>oss-parent</artifactId>
        <version>7</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.nc</groupId>
    <artifactId>nativeCriteria-root</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>NativeCriteria - Root</name>
    <description>Native Criteria Root</description>

   <!-- <modules>
        <module>nativeCriteria-core</module>
        <module>nativeCriteria-si</module>
    </modules>-->

    <licenses>
        <license>
            <name>The Apache Software License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>pnowy</id>
            <name>Przemek Nowak</name>
            <email>przemek.nowak.pl@gmail.com</email>
        </developer>
        <developer>
            <id>kjasek</id>
            <name>Kamil Jasek</name>
            <email>kamil.jasek@gmail.com</email>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/pnowy/NativeCriteria</connection>
        <developerConnection>scm:git:git://github.com/pnowy/NativeCriteria</developerConnection>
        <url>https://github.com/pnowy/NativeCriteria</url>
    </scm>

    <!-- FIRST APPROACH
    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub ${project.artifactId} Repository</name>
            <url>https://raw.github.com/pnowy/${project.artifactId}/mvn-repo</url>
        </repository>
    </distributionManagement>
    -->

    <!-- SECOND APPROACH
    <distributionManagement>
        <repository>
            <id>repo</id>
            <url>https://github.com/pnowy/NativeCriteria/raw/master/releases</url>
        </repository>
        <snapshotRepository>
            <id>snapshot-repo</id>
            <url>https://github.com/pnowy/NativeCriteria/raw/master/snapshots</url>
        </snapshotRepository>
    </distributionManagement>
    -->

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- BASE NATIVE LIBRARY -->
        <hibernate.core.version>4.2.3.Final</hibernate.core.version>
        <!-- LOGGING -->
        <slf4j.version>1.7.2</slf4j.version>
        <log4j.version>1.2.17</log4j.version>
        <!-- UTILS -->
        <guava.version>14.0.1</guava.version>
        <apache.commons.lang.version>3.1</apache.commons.lang.version>
        <apache.commons.collections.version>3.2.1</apache.commons.collections.version>
        <!-- TESTING -->
        <testng.version>6.8.5</testng.version>
        <hsqldb.version>2.2.9</hsqldb.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!--BASE NATIVE LIBRARY -->
            <dependency>
                <groupId>org.hibernate</groupId>
                <artifactId>hibernate-core</artifactId>
                <version>${hibernate.core.version}</version>
            </dependency>
            <!-- UTILS -->
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${apache.commons.lang.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-collections</groupId>
                <artifactId>commons-collections</artifactId>
                <version>${apache.commons.collections.version}</version>
            </dependency>
            <!-- Logging -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-log4j12</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>jcl-over-slf4j</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>log4j</groupId>
                <artifactId>log4j</artifactId>
                <version>${log4j.version}</version>
            </dependency>
            <!-- TESTNG -->
            <dependency>
                <groupId>org.testng</groupId>
                <artifactId>testng</artifactId>
                <version>${testng.version}</version>
                <scope>test</scope>
            </dependency>
            <!-- HSQLDB FOR dbUnit TESTS -->
            <dependency>
                <groupId>org.hsqldb</groupId>
                <artifactId>hsqldb</artifactId>
                <version>${hsqldb.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>2.3.2</version>
                <configuration>
                    <source>1.6</source>
                    <target>1.6</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>2.4.1</version>
            </plugin>
            <plugin>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>2.7</version>
                <configuration>
                    <!--SECOND APPROACH - ARTIFACTS IN TRUNK -->
                    <!--<altDeploymentRepository>snapshot-repo::default::file://${project.basedir}/snapshots</altDeploymentRepository>-->
                    <altDeploymentRepository>release-repo::default::file://${project.basedir}/releases</altDeploymentRepository>

                    <!-- FIRST APPROACH - ARTIFACTS IN BRANCH -->
                    <!--<altDeploymentRepository>internal.repo::default::file://${project.build.directory}/mvn-repo</altDeploymentRepository>-->
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.github.github</groupId>
                <artifactId>site-maven-plugin</artifactId>
                <version>0.8</version>
                <configuration>
                    <!-- git commit message -->
                    <message>Maven artifacts for ${project.version}</message>
                    <!-- disable webpage processing -->
                    <noJekyll>true</noJekyll>
                    <!-- matches distribution management repository url above -->
                    <outputDirectory>${project.build.directory}/mvn-repo</outputDirectory>
                    <!-- remote branch name -->
                    <branch>refs/heads/mvn-repo</branch>
                    <includes>
                        <include>**/*</include>
                    </includes>
                    <!-- github repo name -->
                    <repositoryName>NativeCriteria</repositoryName>
                    <!-- github username -->
                    <repositoryOwner>pnowy</repositoryOwner>
                </configuration>
                <executions>
                    <!-- run site-maven-plugin's 'site' target as part of the build's normal 'deploy' phase -->
                    <execution>
                        <goals>
                            <goal>site</goal>
                        </goals>
                        <!-- UNCOMMENT TO FIRST APPROACH -->
                        <!-- <phase>deploy</phase>-->
                    </execution>
                </executions>
            </plugin>
        </plugins>

    </build>

    <!--
    <profiles>
        <profile>
            <id>deploy-snapshot</id>
            <properties>
                <altDeploymentRepository>snapshot-repo::default::file://${project.basedir}/snapshots</altDeploymentRepository>
            </properties>
        </profile>
        <profile>
            <id>deploy-release</id>
            <properties>
                <altDeploymentRepository>release-repo::default::file://${project.basedir}/releases</altDeploymentRepository>
            </properties>
        </profile>
    </profiles>
    -->

</project>